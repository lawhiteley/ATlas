# ATlas

TODO: write this :) 

If you don't use [mise](https://mise.jdx.dev/) you can just run the commands inside of `mise.toml`.


To install dependencies:
```bash
mise deps
```

To run:
```bash
mise dev 
```

To build:
```bash
mise build
```